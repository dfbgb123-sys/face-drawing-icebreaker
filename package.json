{
  "name": "portrait-party",
  "version": "1.0.0",
  "private": true,
  "description": "In-person icebreaker: everyone draws everyone else's portrait in timed rounds, then reveals results.",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "qrcode": "^1.5.3",
    "socket.io": "^4.7.5"
  },
  "devDependencies": {
    "socket.io-client": "^4.8.3"
  }
}
